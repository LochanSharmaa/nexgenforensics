fastapi>=0.115.0
uvicorn[standard]>=0.30.1
sqlmodel>=0.0.21
alembic>=1.13.1
# [email] extra: imatch_api schemas use pydantic EmailStr for operator
# accounts, which needs email-validator at import time. Declared as an extra
# rather than a bare `pydantic` so a clean install does not fail at startup.
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
httpx>=0.27.0
pyjwt>=2.8.0
bcrypt>=4.2.0
# imatch_api/core/security.py hashes credentials with Argon2id. This was
# imported but never declared, so `imatch_api.main:app` -- the target the
# Dockerfile actually deploys -- failed to import on a clean install.
argon2-cffi>=23.1.0
sentry-sdk[fastapi]>=2.0.0
python-multipart>=0.0.20
pillow>=11.1.0
# Pinned to the line every benchmark in BENCHMARKS.md was produced on.
# The working venv resolved 1.26.4; a clean rebuild resolved 2.4.6, and the
# accuracy numbers were NOT re-verified under 2.x. Floor raised to 1.26.4 and
# capped below 3.0 so the validated numerics cannot silently change.
numpy>=1.26.4,<3.0
cryptography>=44.0.0

reportlab>=4.0  # forensic case report PDF export (imatch_api/services/report_pdf.py)

# Exact inner-product index for 1:N gallery search. Measured 2.4x faster than
# the numpy path at 50k templates with recall 1.000 by construction (exact).
# See BENCHMARKS.md 7d. Approximate indexes were measured and REJECTED.
faiss-cpu>=1.8

# Postgres driver (psycopg 3). Required for hosted deployments where SQLite on
# a persistent disk is not available -- see render.yaml. imatch_api/db/session.py
# rewrites postgres:// and postgresql:// URLs to postgresql+psycopg:// so a
# connection string pasted from a provider dashboard works unmodified.
psycopg[binary]>=3.1
