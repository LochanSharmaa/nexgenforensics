# Capacity Validation Report

Generated by `backend/scripts/validate_capacity.py`. CPU only.

Identity capacity is the number of bits of identity information an observation delivers against a reference population. It bounds the gallery size at which identification remains possible **for any algorithm**, which is a stronger and more useful statement than an accuracy figure.

It is also fragile. This report records exactly where it is trustworthy.


## Verdict summary

| dataset | identity labels | capacity valid? | reason |
|---|---|---|---|
| lfw | no | **NO** | protocol pack ships pairs only; no identity-disjoint population constructible |
| cfp_ff | no | **NO** | protocol pack ships pairs only; no identity-disjoint population constructible |
| cfp_fp | no | **NO** | protocol pack ships pairs only; no identity-disjoint population constructible |
| agedb_30 | no | **NO** | protocol pack ships pairs only; no identity-disjoint population constructible |
| calfw | no | **NO** | protocol pack ships pairs only; no identity-disjoint population constructible |
| cplfw | no | **NO** | protocol pack ships pairs only; no identity-disjoint population constructible |
| tinyface | **yes** | **YES** | identities recoverable from filenames; population built by rejection sampling |
| qmul_survface | yes | blocked | folder-per-identity, but embeddings not cached (GPU) |
| ijb_b / ijb_c | yes | blocked | meta present, embeddings not cached (GPU) |

## Failure mode 1 -- censoring by impostor sample size

A pool of M impostors cannot resolve a tail below 1/M, so bits are capped at log2(M). Against each pack's own ~3,000 impostor pairs the estimator saturates and reports the ceiling instead of a measurement.

| dataset | impostors | ceiling (bits) | censored |
|---|---|---|---|
| lfw | 3,000 | 11.551 | **99.7%** |
| cfp_ff | 3,500 | 11.774 | **99.8%** |
| cfp_fp | 3,500 | 11.774 | **94.5%** |
| agedb_30 | 3,000 | 11.551 | **93.7%** |
| calfw | 3,000 | 11.551 | **89.5%** |
| cplfw | 3,000 | 11.551 | **44.0%** |

LFW at 99.7% censored is not a noisy estimate. It is no estimate at all.


## Failure mode 2 -- contamination of a sampled pool

The obvious fix is a larger pool built by randomly pairing images from different pairs. This assumes *different pair implies different identity*, which is false: these packs draw thousands of pairs from a few hundred identities.

| dataset | identities | suspect fraction (>0.5) | verdict |
|---|---|---|---|
| cfp_ff | 500 | 0.181% | unlabelled |
| cfp_fp | 500 | 0.153% | unlabelled |
| agedb_30 | 568 | 0.202% | unlabelled |
| cplfw | 3,884 | 0.038% | unlabelled |
| calfw | 4,025 | 0.020% | unlabelled |
| lfw | 5,749 | 0.030% | unlabelled |

Contamination tracks identity count exactly, as the mechanism predicts. **0.2% contamination is twice the FAR being measured at 0.1%**, so the tail is dominated by mislabelled genuine pairs. Using this pool made AgeDB-30 appear to fall from 96.03% to 8.40% TAR@FAR=0.1% -- an artefact.

`nexgen_engine.forensics.population.audit_population` now returns `unlabelled` for any pool of this construction, and `ReferencePopulation.from_labelled` raises rather than returning a usable object without identity labels.


## Valid measurements

### TinyFace (reference population: `tinyface_testset`)

- population verdict: **valid** — 19,984,982 identity-disjoint comparisons (probe 0.6966); supports claims to FAR 5.00376e-07.
- genuine comparisons: 20,888
- censored fraction: 1.70%

| statistic | value | 95% CI |
|---|---|---|
| bits (median) | 4.328 | [4.257, 4.397] |
| bits (20th pct) | 1.639 | [1.592, 1.685] |
| supportable gallery @50% rank-1 | 10 | — |
| supportable gallery @80% rank-1 | 2 | — |
| supportable gallery @90% rank-1 | 1 | — |

**Interpretation.** At surveillance resolution this system does not carry enough identity information to support 1:N identification at any operationally meaningful gallery size. This is a property of the evidence and the model jointly, not of the decision threshold.


## What is required to extend this

| need | unlocks | blocked on |
|---|---|---|
| Embed 153,428 TinyFace distractors | A real open-set reference population and gallery, instead of a 1,794-entry proxy | GPU |
| Embed QMUL-SurvFace | Second independent native-LR capacity measurement | GPU |
| Extract and embed IJB-B/IJB-C | Capacity at scale with published reference results | GPU |
| Demographically declared cohorts | Case-usable LR denominators rather than corpus-shaped ones | Data collection |

## Standing caveats on every number above

1. **Independence is assumed and is false.** The N*p argument treats gallery members as independent draws; relatives and doppelgangers cluster, so the true supportable gallery is SMALLER than reported. Optimistic direction.

2. **These populations are corpora, not forensic reference populations.** They characterise the system, not any case.

3. **Capacity is a property of (model, population, condition)**, not of the image. It measures what this system recovers, which lower-bounds what the pixels contain.

4. **The TinyFace cache lacks flip-TTA** (`emb` only), so its figures are not directly comparable to published numbers produced with test-time augmentation.

