# Measurement Record — GPU campaign of 2026-08-02

Written to the SCORECARD standard: every number names the artifact that produced
it, every superseded number says what replaced it and why, and every error found
in our own work is recorded rather than papered over.

**Configuration for every figure on this page:** `w600k_r50` (stock InsightFace
ArcFace R50/WebFace600K), flip-TTA unless noted, RTX A3000 Laptop 6 GB via
onnxruntime `CUDAExecutionProvider`.

*Throughput and elapsed-time figures are read from the run logs in
`runtime/logs/` (e.g. `g1_embed.log`, `g3_qmul.log`, `g4_ijbb.log`), which record
img/s and total seconds per job. They are operational notes, not measurements
carried in any result artifact, and no total is quoted here as final while runs
remain outstanding. `GPU_EXECUTION_REQUEST.md` states a **requested** budget of
5.5–9 GPU-hours; no approval artifact exists in the repo and none is implied.*

---

## 1. Harness validation — IJB-B / IJB-C (G4)

Official protocol throughout: 5-point `face_align.norm_crop`, media-aware
template pooling per `IJB_11.py`, official pair lists.

| | IJB-B | IJB-C | artifact |
|---|---|---|---|
| faces | 227,630 | 469,375 | `runtime/forensics/ijb_ijbb.json`, `ijb_ijbc.json` |
| templates | 12,115 | 23,124 | |
| pairs | 8,010,270 | 15,658,489 | |
| TAR @ FAR=1e-4 | **95.44%** | **96.91%** | |
| TAR @ FAR=1e-5 | 91.09% | 94.44% | |
| TAR @ FAR=1e-3 | 97.03% | 98.13% | |

Published ArcFace (R100/MS1MV2) sits at ~94–95% (IJB-B) and ~96–97% (IJB-C) at
FAR=1e-4. **This band is recalled from the literature and is not cited to a
verified source in this repository** — it is the sole external anchor for the
harness-validation claim, so it should be pinned to a specific paper and table
before the claim is relied on outside the team. Reproducing that band with a *smaller* backbone on the official
protocol is the validation the harness needed: **our numbers are not harness
artifacts.** (IJB-B @1e-6 = 43.43% rests on 8 impostor pairs and should not be
quoted.)

This closes SCORECARD item 8, whose "1.57 GB partial download" label was stale —
the complete 8.6 GB suite was in `Downloads/ijb-testsuite.tar`.

## 2. Capacity and open-set, official protocols (G1 + G3)

Two corpora, complementary by structure — TinyFace has a 153k-distractor gallery
but **zero unmated probes**; QMUL has true unmated probes but a small gallery.
Neither substitutes for the other.

| | TinyFace | QMUL-SurvFace | artifact |
|---|---|---|---|
| protocol | Gallery_Match × Probe + Gallery_Distractor | official Face_Identification_Test_Set | `capacity_official_*.json` |
| genuine pairs | 12,308 | 500,000 | |
| impostor pool | 50M, audit `valid` | 50M, audit **`label_uncertainty`** (2.31% suspect) | |
| identity bits, median | **4.73** | **2.21** | |
| identity bits, p20 | 1.89 | 0.84 | |
| supportable gallery @80% rank-1 | **≈ 2** | **≈ 1** | |
| TAR @ FAR=0.1% | 20.59% | **2.31%** | |
| TAR @ FAR=1e-5 | 7.38% | 0.11% | |
| rank-1 (155,997 gallery / 2,965 gallery) | **32.93%** | **2.68%** | |
| TPIR @ 1% FPIR | *unmeasurable — no unmated probes* | **0.00%** | |
| TPIR @ 10% FPIR | — | 0.17% | |
| Cllr (held-out) | 0.6662 | **0.9047** | |
| Cllr_cal (recoverable) | 0.0040 (0.6%) | 0.0013 (0.1%) | |

**On the QMUL pool's audit verdict.** Its `population_audit.verdict` is
`label_uncertainty`, not `valid`: 2.31% of cross-identity comparisons exceed the
95th percentile of genuine scores. Identity disjointness is guaranteed by the
protocol (verified in `test_protocol_invariants.py`), so these are not pairing
errors — but on 25-pixel imagery duplicate identity labels and genuinely
confusable people are not separable by score, so the flag is carried rather than
dismissed. It makes QMUL's already-poor numbers, if anything, slightly
optimistic.

**The two headline findings:**

1. **On real cross-camera surveillance (QMUL), the stock model cannot do
   open-set identification at all.** TPIR 0.00% at 1% FPIR with 1,844 genuine
   unmated probes; Cllr 0.90 means ~0.1 bits delivered of a 1-bit question.
   This is now *measured with true non-mates*, not extrapolated.
2. **Calibration is exhausted everywhere.** Cllr_cal ≤ 0.6% of the loss on both
   corpora. Nothing reportable is recoverable by a better evidence layer alone;
   the deficit is discrimination, and only a better recognition model moves it.

### Superseded numbers, and why

Diagnostic figures below are measured in `runtime/forensics/provenance_diagnostics.json`
(regenerated by `backend/scripts/generate_provenance_artifacts.py`).

| withdrawn figure | replacement | cause |
|---|---|---|
| TinyFace rank-1 **37.43%** (`capacity_real_gallery.json`) | 32.93% | genuine pairs built by sort order; 39.4% were Gallery×Gallery near-duplicate track frames (mean 0.4225 vs official 0.3268, inflation +0.0957 — §A) |
| TinyFace TPIR@1% 9.60% | unmeasurable | same run; TinyFace has no unmated probes, so its "TPIR" measured nothing |
| QMUL rank-1 0.53% (first official run, superseded in place) | 2.68% | unmated probes wrongly placed in the gallery (95,837 instead of 2,965). Locked by `test_protocol_invariants.py` |
| All capacity figures from `analyze_evidence_capacity.py` on protocol packs | none — refused | impostor pools without identity labels: saturation (LFW 99.7% censored) then contamination (AgeDB 0.202% of pairs scoring >0.5 in an *unlabelled* pool, against a 0.1% FAR target — §B/C. Score is a proxy for same-person here, the same inference we decline to make for QMUL; it is used only to reject a pool, never to accept one) |

## 3. S0.3 — the forward-operator go/no-go (G2)

**Question:** does modelling the probe's imaging operator and applying it
forward to the gallery beat simple resolution matching (B1)? Decision rule
registered in code before any run: PASS ≥ +2.0 pts TAR@FAR=0.1% with 95% CI
excluding zero; FAIL ≤ +0.5 or CI spanning zero.

**Two arms exist because the first was wrong in a way worth keeping visible:**
B2 applies the probe's *absolute* estimated operator; B2r applies only the
*residual* beyond what the gallery already carries (variances in quadrature;
JPEG only when strictly coarser).

TAR@FAR=0.1%, `experiments/S0_3/results/s0_3_arcface_*.json`:

| dataset | A | B1 | B2 (absolute) | **B2r (residual)** | B3 | C | B2r−B1 [CI] | verdict |
|---|---|---|---|---|---|---|---|---|
| tinyface (3k pairs) | 25.33 | 25.40 | 17.40 | **25.53** | 19.33 | 0.73 | **+0.13 [−0.41, +1.84]** | **FAIL** |
| qmul (3k pairs) | 8.80 | 6.47 | 1.73 | **6.60** | 3.07 | 0.07 | **+0.13 [−2.20, +1.53]** | **FAIL** |
| lfw_synth (20k pairs) | 2.37 | 2.13 | 3.43 | **5.68** | 4.04 | 0.50 | **+3.55 [+2.02, +5.02]** | **PASS** |

### Reading the verdict honestly

- **Surveillance-to-surveillance: the hypothesis fails, correctly.** B2's −8.0
  pts was double degradation (both sides already share the operator; the
  residual is ≈0). B2r confirms it: the correct forward model reduces to a
  no-op, +0.13 pts, CI spanning zero. *The method is coherent but inapplicable
  where no operator asymmetry exists.*
- **HR-gallery vs degraded-probe: the one PASS.** +3.55 pts on `lfw_synth` with
  CI excluding zero, and **B2r beats B2 on every dataset** (5.68 vs 3.43 here;
  25.53 vs 17.40 and 6.60 vs 1.73 on the surveillance corpora) — the residual
  formulation is strictly better than the absolute one everywhere, which is the
  independent confirmation that the double-degradation diagnosis was right. Caveat that only new data removes: the degradation is
  synthetic and B2 estimates a simplified version of the same operator family,
  so part of the margin may be self-fulfilment. **SCface (real mugshot vs real
  CCTV at 3 known distances) is the decisive test** — see
  `DATA_ACQUISITION_REQUEST.md`.
- **Pixel-space likelihood (C) is dead everywhere** (AUC 0.55–0.60).
- **Consequence for the roadmap:** Stage 3 (generative core) is **refuted** for
  surveillance-to-surveillance and **conditionally licensed** for the asymmetric
  mugshot-vs-CCTV case, on synthetic degradation only. SCface converts that
  condition into a decision.

## 4. Defects found in our own work during the campaign

All found by our own validation and fixed the same day. Regression coverage is
split across two files and is **not** uniform — stated explicitly because an
audit of an earlier draft of this page found it implied otherwise:

- defects 1–6 → `test_degradation_regressions.py` (19 tests)
- defects 7–8 → `test_protocol_invariants.py` (6 tests)

Section markers **§A–§F** below index `runtime/forensics/provenance_diagnostics.json`.

An audit of an earlier draft found the first defect-8 test was *fake*: its
docstring claimed it asserted the straddling property, its assertions were
`len(G)==40` and `ndim==3`, and it imported the S0.3 loader rather than the
capacity loader that produced the withdrawn number. It was replaced with one
asserting exact row counts (4,443 / 3,728) and the recorded 12,308 genuine pairs
on the correct module. A test that looks like a lock and is not is worse than no
test, and it is recorded here because it was our own error.

| # | defect | observed effect | fix |
|---|---|---|---|
| 1 | `estimate_quality` recompression-MSE criterion | returned **q=95 for true q=35 at 31, 48, 112 and 250 px** — every size tested (§F) | DCT-lattice fitting; **24/24 exact** across q∈{20,35,50,65,80,95} × {31,48,112,250}px (§D) |
| 2 | confidence = best-vs-second-best | ~0 margin even when exactly right | best-vs-median: **0.880** compressed vs **0.021** raw; 100% accept / 0% false-accept at the 0.15 gate (§E) |
| 3 | channel averaging before JPEG estimation | superimposed three quantisation lattices; **76.0%** accuracy over 100 cases (§F) | green-channel estimation; **100.0%** (§F) |
| 4 | `trustworthy` = blur-R² alone | flag True while JPEG estimate wrong by 60 points | conjunction with per-component breakdown |
| 5 | `arm_B2` unit order | probe-space σ applied before decimation → 1/8 of the operator | decimate first, apply in probe space |
| 6 | `arm_B2` absolute operator | double degradation, −8.0 pts | `arm_B2r` residual composition |
| 7 | QMUL unmated probes placed in gallery | rank-1 0.53% artifact | roles enforced per protocol (`non_role`) |
| 8 | TinyFace genuine pairs by sort order | rank-1 inflated to 37.43% | official Gallery_Match × Probe only |

The pattern across all eight: **the comparison set must be constructed by
protocol, not by convenience, and estimated quantities must be validated against
known ground truth before they are trusted.** Both are now enforced in code
(population purity refusal; trust-component gating) rather than by discipline.

## 5. What this campaign changes

1. **The harness reproduces published-ArcFace results** on IJB-B/C. Stated
   precisely: the comparison band is recalled from literature and uncited (§1),
   so this is internal consistency with a remembered figure, not third-party
   validation. L2 stands unchanged.
2. **The capacity framework has real reference populations** on both surveillance
   corpora, identity-disjoint by protocol construction — with the caveat from §2
   carried forward: QMUL's pool audits as `label_uncertainty` (2.31% suspect),
   not `valid`, which if anything makes its already-poor numbers optimistic.
3. **The open-set deficit is measured, not assumed**: TPIR 0.00% @1% FPIR on QMUL.
4. **The generative-core decision is data-driven and now complete for the data we
   hold**: the residual forward-operator model **PASSES** on HR-gallery/LR-probe
   (+3.55 pts, CI [+2.02, +5.02]) and is a measured **no-op** on
   surveillance-to-surveillance (+0.13 on both corpora, CI spanning zero). It is
   therefore licensed *narrowly* — for the mugshot-vs-CCTV case, which is the
   forensic one — and refuted for the camera-to-camera case. The remaining threat
   to the PASS is that `lfw_synth` degradation is synthetic and B2r estimates a
   simplified form of the operator we applied; **SCface is the test that settles
   it** (`DATA_ACQUISITION_REQUEST.md`).
5. **The discrimination bottleneck is confirmed three independent ways** (Cllr
   decomposition, capacity bits, open-set TPIR). The next accuracy move is the
   backbone swap (ViT-B + KP-RPE + AdaFace; published TinyFace R1 76.10 — *uncited, see caveat in §1* — vs our
   protocol-corrected 32.93 rank-1), not more evidence-layer work.
